<?php
// 소스 코드는 분류화해서 파일을 나눈 후 관리를 하므로 필요에 따라 아래 지시문을 통해 불러와서 사용
// include : 같은 파일 여러 번 불러올 수 있음 / 포함할 파일이 없어도 다음 코드 실행(Warning)
// include_once : 같은 파일 한 번만 불러옴 / 포함할 파일이 없어도 다음 코드 실행(Warning)
// require : 같은 파일 여러 번 불러올 수 있음 / 포함할 파일이 없으면 다음 코드 실행하지 않음(Fatal Error)
// require_once : 같은 파일 한 번만 불러옴 / 포함할 파일이 없으면 다음 코드 실행하지 않음(Fatal Error)
require_once("999_93_config.php");

require_once("999_91_inc.php");
require_once("999_94_inc.php");